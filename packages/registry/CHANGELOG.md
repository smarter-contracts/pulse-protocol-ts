# Changelog — @pulse-protocol/registry

## [0.1.0] - 2026-07-31

### Added

- `ServiceRegistry` interface binding grantee counterparty DIDs to authenticated service identifiers
- `ServiceBinding` and `BindingStatus` types, and `BindingNotFoundError`
- `MemoryServiceRegistry` in-memory reference implementation with upsert rotation
